const producto = "Monitor de 20\"";

const precio = '30 usd'

console.log(producto.concat(precio));
console.log(producto.concat("en descuento"));
console.log(producto +" "+ precio);
console.log(`el producto ${producto} tiene un  ${precio}`);